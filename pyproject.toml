[tool.poetry]
name = "demo"
version = "0.1.0"
description = ""
authors = ["galbane-code <gal.baneim@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
